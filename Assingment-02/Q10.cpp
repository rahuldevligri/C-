/*
Write a C++ program to check whether a given character is
a digit, lowercase alphabet, uppercase alphabet or a special
character.
*/
#include<iostream>
#include<conio.h>

using namespace std;

int main()
{
    char n;
    cout<<"Enter a Char "; //predefined Object Output stream << insertion operator
    cin>>n;  //predefined Object Input stream  >> Extraction operator

        cout<<n;
        if (n>=0 || n<=0)
        cout<<"Number";
        else if(n>=a || n<=z)
           
getch();
}